
### aggregateCosine aggregates the frequencies to calculate the distances
### between the lects with the cosine similarity metric. This is a naive 
### approach that ignores the semantic ties between the variants.

aggregateCosine <- function(t.init){
	## nothing yet
}
